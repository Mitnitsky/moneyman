diff --git a/node_modules/israeli-bank-scrapers/lib/definitions.js b/node_modules/israeli-bank-scrapers/lib/definitions.js
index e9fe28e..0321291 100644
--- a/node_modules/israeli-bank-scrapers/lib/definitions.js
+++ b/node_modules/israeli-bank-scrapers/lib/definitions.js
@@ -31,7 +31,7 @@ let CompanyTypes = exports.CompanyTypes = /*#__PURE__*/function (CompanyTypes) {
 const SCRAPERS = exports.SCRAPERS = {
   [CompanyTypes.hapoalim]: {
     name: 'Bank Hapoalim',
-    loginFields: ['userCode', PASSWORD_FIELD]
+    loginFields: ['userCode', PASSWORD_FIELD, 'otpCodeRetriever', 'phoneNumber']
   },
   [CompanyTypes.leumi]: {
     name: 'Bank Leumi',
diff --git a/node_modules/israeli-bank-scrapers/lib/helpers/elements-interactions.js b/node_modules/israeli-bank-scrapers/lib/helpers/elements-interactions.js
index efd9183..8441a72 100644
--- a/node_modules/israeli-bank-scrapers/lib/helpers/elements-interactions.js
+++ b/node_modules/israeli-bank-scrapers/lib/helpers/elements-interactions.js
@@ -40,12 +40,25 @@ async function waitUntilIframeFound(page, framePredicate, description = '', time
   return frame;
 }
 async function fillInput(pageOrFrame, inputSelector, inputValue) {
-  await pageOrFrame.$eval(inputSelector, input => {
-    const inputElement = input;
-    // @ts-ignore
-    inputElement.value = '';
+  await pageOrFrame.click(inputSelector, {
+    clickCount: 3
+  });
+  await new Promise(r => setTimeout(r, 100));
+  await pageOrFrame.type(inputSelector, inputValue, {
+    delay: 75
   });
-  await pageOrFrame.type(inputSelector, inputValue);
+  await new Promise(r => setTimeout(r, 200));
+  const actual = await pageOrFrame.$eval(inputSelector, el => el.value);
+  if (actual !== inputValue) {
+    await pageOrFrame.click(inputSelector, {
+      clickCount: 3
+    });
+    await new Promise(r => setTimeout(r, 100));
+    await pageOrFrame.type(inputSelector, inputValue, {
+      delay: 75
+    });
+    await new Promise(r => setTimeout(r, 200));
+  }
 }
 async function setValue(pageOrFrame, inputSelector, inputValue) {
   await pageOrFrame.$eval(inputSelector, (input, value) => {
diff --git a/node_modules/israeli-bank-scrapers/lib/helpers/fetch.js b/node_modules/israeli-bank-scrapers/lib/helpers/fetch.js
index 618bf63..38400aa 100644
--- a/node_modules/israeli-bank-scrapers/lib/helpers/fetch.js
+++ b/node_modules/israeli-bank-scrapers/lib/helpers/fetch.js
@@ -58,7 +58,22 @@ async function fetchGraphql(url, query, variables = {}, extraHeaders = {}) {
   }
   return result.data;
 }
+async function simulateHumanBehavior(page) {
+  const delay = 1000 + Math.floor(Math.random() * 3000);
+  await new Promise(resolve => setTimeout(resolve, delay));
+  try {
+    const viewport = page.viewport();
+    if (viewport) {
+      const x = Math.floor(Math.random() * viewport.width);
+      const y = Math.floor(Math.random() * viewport.height);
+      await page.mouse.move(x, y, {
+        steps: 5 + Math.floor(Math.random() * 10)
+      });
+    }
+  } catch (e) {}
+}
 async function fetchGetWithinPage(page, url, ignoreErrors = false) {
+  await simulateHumanBehavior(page);
   const [result, status] = await page.evaluate(async innerUrl => {
     let response;
     try {
@@ -88,6 +103,7 @@ async function fetchGetWithinPage(page, url, ignoreErrors = false) {
   return null;
 }
 async function fetchPostWithinPage(page, url, data, extraHeaders = {}, ignoreErrors = false) {
+  await simulateHumanBehavior(page);
   const [resultText, status] = await page.evaluate(async (innerUrl, innerData, innerExtraHeaders) => {
     const response = await fetch(innerUrl, {
       method: 'POST',
diff --git a/node_modules/israeli-bank-scrapers/lib/scrapers/base-isracard-amex.js b/node_modules/israeli-bank-scrapers/lib/scrapers/base-isracard-amex.js
index 8125550..8c141e6 100644
--- a/node_modules/israeli-bank-scrapers/lib/scrapers/base-isracard-amex.js
+++ b/node_modules/israeli-bank-scrapers/lib/scrapers/base-isracard-amex.js
@@ -250,15 +250,11 @@ class IsracardAmexBaseScraper extends _baseScraperWithBrowser.BaseScraperWithBro
     const loginStartTime = performance.now();
     await this.page.setRequestInterception(true);
     this.page.on('request', request => {
-      if (request.url().includes('detector-dom.min.js')) {
-        debug('force abort for request do download detector-dom.min.js resource');
-        void request.abort(undefined, _browser.interceptionPriorities.abort);
-      } else {
-        void request.continue(undefined, _browser.interceptionPriorities.continue);
-      }
+      void request.continue(undefined, _browser.interceptionPriorities.continue);
     });
     await (0, _browser.maskHeadlessUserAgent)(this.page);
     await this.navigateTo(`${this.baseUrl}/personalarea/Login`);
+    await (0, _waiting.sleep)(5000);
     this.emitProgress(_definitions.ScraperProgressTypes.LoggingIn);
     const validateUrl = `${this.servicesUrl}?reqName=ValidateIdData`;
     const validateRequest = {
diff --git a/node_modules/israeli-bank-scrapers/lib/scrapers/hapoalim.js b/node_modules/israeli-bank-scrapers/lib/scrapers/hapoalim.js
index 4ffa0b2..bda9a26 100644
--- a/node_modules/israeli-bank-scrapers/lib/scrapers/hapoalim.js
+++ b/node_modules/israeli-bank-scrapers/lib/scrapers/hapoalim.js
@@ -177,6 +177,48 @@ function createLoginFields(credentials) {
     value: credentials.password
   }];
 }
+async function handleOtpIfNeeded(page, credentials) {
+  debug('racing OTP dialog vs redirect...');
+  try {
+    const result = await Promise.race([page.waitForSelector('auth-otp-login', {
+      timeout: 30000
+    }).then(() => 'otp'), (0, _navigation.waitForRedirect)(page, 30000).then(() => 'redirect')]);
+    if (result === 'redirect') {
+      debug('redirected without OTP, login successful');
+      return;
+    }
+    debug('OTP dialog detected!');
+    if (!credentials.otpCodeRetriever) {
+      debug('no otpCodeRetriever provided, waiting for manual OTP entry...');
+      await (0, _navigation.waitForRedirect)(page, 300000);
+      return;
+    }
+    debug('requesting OTP code via otpCodeRetriever...');
+    const otpCode = await credentials.otpCodeRetriever();
+    debug('got OTP code, entering into form...');
+    if (!otpCode || otpCode.length < 5) {
+      throw new Error('Invalid OTP code received (must be at least 5 digits)');
+    }
+    for (let i = 0; i < Math.min(otpCode.length, 5); i++) {
+      const selector = `[data-testid="separated-${i}"]`;
+      await page.waitForSelector(selector, {
+        timeout: 5000
+      });
+      await page.click(selector);
+      await page.type(selector, otpCode[i]);
+      await (0, _waiting.sleep)(100);
+    }
+    debug('clicking submit...');
+    await (0, _waiting.sleep)(500);
+    await page.click('button.btn-red_1');
+    debug('waiting for redirect after OTP...');
+    await (0, _navigation.waitForRedirect)(page, 30000);
+    debug('OTP flow completed successfully');
+  } catch (e) {
+    debug('OTP handling error: %s', e.message);
+    throw e;
+  }
+}
 class HapoalimScraper extends _baseScraperWithBrowser.BaseScraperWithBrowser {
   get baseUrl() {
     return 'https://login.bankhapoalim.co.il';
@@ -186,7 +228,7 @@ class HapoalimScraper extends _baseScraperWithBrowser.BaseScraperWithBrowser {
       loginUrl: `${this.baseUrl}/cgi-bin/poalwwwc?reqName=getLogonPage`,
       fields: createLoginFields(credentials),
       submitButtonSelector: '.login-btn',
-      postAction: async () => (0, _navigation.waitForRedirect)(this.page),
+      postAction: async () => handleOtpIfNeeded(this.page, credentials),
       possibleResults: getPossibleLoginResults(this.baseUrl)
     };
   }
